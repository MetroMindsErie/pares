"use client";
import React from 'react';

// This component should not render a map - confirm it's empty
const SearchBar = ({ onSearchResults }) => {
  return null; // Ensure this returns null so no map is rendered here
};

export default SearchBar;
